#!/sbin/openrc-run

command="/usr/bin/webmon"
command_args="${WEBMON_OPTS:-${WBOPTS:--H 127.0.0.1 -p 61080 -w 2}}"
pidfile="/run/webmon.pid"
command_background="yes"
name="webmon"

depend() {
    need net
}

start_pre() {
    checkpath --directory /run
}
